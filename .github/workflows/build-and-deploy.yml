name: Build Solana Program

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential pkg-config libssl-dev libudev-dev wget bzip2

      - name: Install Rust 1.75
        uses: dtolnay/rust-toolchain@1.75.0

      - name: Download Solana 1.18.22
        run: |
          wget -q https://github.com/solana-labs/solana/releases/download/v1.18.22/solana-release-x86_64-unknown-linux-gnu.tar.bz2 -O solana.tar.bz2
          tar -xjf solana.tar.bz2
          mkdir -p $HOME/.local/share/solana/install/active_release
          cp -r solana-release/* $HOME/.local/share/solana/install/active_release/
          rm -rf solana-release solana.tar.bz2
          echo "$HOME/.local/share/solana/install/active_release/bin" >> $GITHUB_PATH

      - name: Fix dependencies for Rust 1.75
        run: |
          export PATH="$HOME/.local/share/solana/install/active_release/bin:$PATH"
          
          # Remove existing Cargo.lock
          rm -f Cargo.lock
          
          # Generate fresh Cargo.lock
          cargo generate-lockfile
          
          # Downgrade all packages that require Rust > 1.75
          # borsh 1.6.0 requires 1.77
          cargo update -p borsh --precise 1.5.1 || true
          
          # toml_datetime 0.7.4 requires 1.76
          cargo update -p toml_datetime --precise 0.6.5 || true
          
          # toml_edit might also need downgrade
          cargo update -p toml_edit --precise 0.21.1 || true
          
          # indexmap 2.7+ requires newer Rust
          cargo update -p indexmap --precise 2.2.6 || true
          
          # hashbrown might need downgrade
          cargo update -p hashbrown --precise 0.14.3 || true
          
          # zerocopy might need downgrade  
          cargo update -p zerocopy --precise 0.7.32 || true
          
          # winnow might need downgrade
          cargo update -p winnow --precise 0.5.40 || true
          
          echo "=== Dependency versions ==="
          grep -E "^(name|version)" Cargo.lock | head -40

      - name: Build program
        run: |
          export PATH="$HOME/.local/share/solana/install/active_release/bin:$PATH"
          cd programs/keystore
          cargo-build-sbf --sbf-out-dir ../../target/deploy
          
      - name: Verify build
        run: |
          ls -lah target/deploy/
          test -f target/deploy/keystore.so && echo "✓ Build successful!"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: solana-program
          path: |
            target/deploy/keystore.so
            target/deploy/keystore-keypair.json
          
      - name: Show deploy info
        run: |
          export PATH="$HOME/.local/share/solana/install/active_release/bin:$PATH"
          echo "========================================="
          echo "✓ BUILD SUCCESSFUL!"
          echo "========================================="
          echo "Program ID:"
          solana-keygen pubkey target/deploy/keystore-keypair.json
          echo ""
          echo "Deploy: solana program deploy keystore.so"
